jmax_temperature_param_bernacchi <- list(
    alpha_j_norm = list(type = 'Polynomial', coef = c(0.352, 0.022, -3.4e-4) / 0.6895,  units = 'normalized to alpha_j at 25 degrees C'),
    theta_j_norm = list(type = 'Polynomial', coef = c(0.76,  0.018, -3.7e-4) / 0.97875, units = 'normalized to theta_j at 25 degrees C')
)
